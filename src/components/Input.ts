import { styled } from '@/styles';

const Input = styled('input', {
	fontSize: '$0',
	px: 1,
	py: 2,
	borderColor: '$foreground-dark-0',
	borderStyle: 'solid',
	borderRadius: '$3',
});

export default Input;
