import { createTheme } from './stitches';

const theme = createTheme({
  colors: {
    bg: '#333333',
    text: '#fff',
    'bg-alpha-1': '#333333c9',
    'bg-alpha-2': 'rgba(51, 51, 51, 0.94)',
    primary: '#be1d3b',
  },
});

export default theme;
